{
  "name": "sample",
  "version": "1.0.0",
  "description": "your description...",
  "main": "server.js",
  "engines": {
    "node": "14.15.1",
    "npm": "6.14.8"
  },
  "dependencies": {
    "@hapi/hapi": "^20.1.0",
    "nodemon": "^2.0.7",
    "pg": "^8.5.1"
  },
  "scripts": {
    "heroku-postbuild": "npm install && cd client && npm install && npm run build && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  }
}
